{
  "name": "clawsense",
  "version": "0.1.0",
  "private": true,
  "description": "Context-aware trigger layer for Claude Code and OpenClaw.",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "electron": "^36.0.0",
    "typescript": "^5.8.0"
  }
}
